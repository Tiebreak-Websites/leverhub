---
// Hero — pure cinematic treatment. No side rail: the text column owns the left,
// the silhouette in the artwork owns the right. One primary CTA.
---

<section class="hero on-navy" id="top">
  <div class="bg" aria-hidden="true"></div>
  <div class="veil" aria-hidden="true"></div>

  <div class="container inner">
    <div class="intro">
      <h1>
        <span class="line"><span class="line-inner">It starts with <em>you</em>,</span></span>
        <span class="line"><span class="line-inner">not the charts.</span></span>
      </h1>

      <p class="tagline rise" style="--d: 0.4s">Your financial transformation begins from within.</p>

      <p class="descriptor rise" style="--d: 0.5s">
        A personal finance, markets and trading academy that helps you understand your
        situation, learn how money and markets move, take control and build a better
        work-life balance.
      </p>

      <div class="actions rise" style="--d: 0.6s">
        <a href="#hub" class="btn btn-teal">Start My LeverHub Journey <span class="arrow" aria-hidden="true">&rarr;</span></a>
        <a href="#video" class="btn btn-ghost">Watch the intro</a>
      </div>

      <p class="microcopy rise" style="--d: 0.7s">
        <span class="play-dot" aria-hidden="true">
          <svg viewBox="0 0 24 24" width="10" height="10"><path fill="currentColor" d="M8 5.5v13l11-6.5-11-6.5Z"></path></svg>
        </span>
        Start your journey. Not your trades.
      </p>
    </div>
  </div>

  <a href="#video" class="scroll-cue rise" style="--d: 1s" aria-label="Scroll to the introduction video">
    <span class="cue-line" aria-hidden="true"></span>
    Scroll
  </a>
</section>

<style>
  .hero {
    position: relative;
    display: grid;
    align-items: center;
    min-height: min(92vh, 880px);
    overflow: hidden;
    background: var(--navy-deep);
    color: var(--white);
    padding-top: clamp(7rem, 11vw, 9rem);
    padding-bottom: clamp(5rem, 8vw, 7rem);
  }

  .bg {
    position: absolute;
    inset: 0;
    background-image: url("/images/hero.jpg");
    background-size: cover;
    background-position: 68% center;
    animation: kenburns 26s ease-in-out infinite alternate;
    will-change: transform;
  }

  @keyframes kenburns {
    from {
      transform: scale(1);
    }
    to {
      transform: scale(1.07);
    }
  }

  /* Text side dark, image side open — the silhouette stays visible */
  .veil {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(90deg, rgba(14, 39, 80, 0.92) 0%, rgba(14, 39, 80, 0.66) 38%, rgba(14, 39, 80, 0.12) 68%, rgba(14, 39, 80, 0.3) 100%),
      linear-gradient(180deg, rgba(14, 39, 80, 0.5) 0%, rgba(14, 39, 80, 0) 30%, rgba(14, 39, 80, 0.55) 100%);
  }

  .inner {
    position: relative;
  }

  .intro {
    display: grid;
    gap: 1.5rem;
    justify-items: start;
    max-width: 620px;
  }

  /* Staggered line-mask headline entrance */
  .line {
    display: block;
    overflow: hidden;
    /* room for descenders inside the mask without changing line rhythm */
    padding-bottom: 0.14em;
    margin-bottom: -0.14em;
  }

  .line-inner {
    display: block;
    transform: translateY(115%);
    transition: transform 0.9s var(--ease-out);
  }

  html.js .in .line-inner,
  html:not(.js) .line-inner {
    transform: none;
  }

  .line:nth-child(2) .line-inner {
    transition-delay: 0.12s;
  }

  .tagline {
    font-family: var(--font-sub);
    font-size: clamp(1.15rem, 1.9vw, 1.35rem);
    font-weight: 600;
    color: var(--white);
  }

  .descriptor {
    color: var(--white-75);
    font-size: 1.05rem;
    line-height: 1.7;
    max-width: 52ch;
  }

  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.9rem;
    margin-top: 0.5rem;
  }

  .microcopy {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    letter-spacing: 0.08em;
    color: var(--white-75);
  }

  .play-dot {
    display: grid;
    place-items: center;
    width: 22px;
    height: 22px;
    border-radius: 50%;
    border: 1px solid rgba(255, 255, 255, 0.4);
    color: var(--white);
  }

  /* Quiet scroll cue, bottom-left */
  .scroll-cue {
    position: absolute;
    left: max(calc((100% - var(--container)) / 2), 1.5rem);
    bottom: 2rem;
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.24em;
    text-transform: uppercase;
    color: var(--white-58);
    transition: color 0.2s ease;
  }

  .scroll-cue:hover {
    color: var(--white);
  }

  .cue-line {
    display: block;
    width: 44px;
    height: 1px;
    background: rgba(255, 255, 255, 0.35);
    overflow: hidden;
    position: relative;
  }

  .cue-line::after {
    content: "";
    position: absolute;
    inset: 0;
    background: var(--teal);
    transform: translateX(-100%);
    animation: cue 2.4s var(--ease-out) infinite;
  }

  @keyframes cue {
    0% {
      transform: translateX(-100%);
    }
    55% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(100%);
    }
  }

  /* Load-in rise for hero content (fires when the section gets .in) */
  .rise {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.8s var(--ease-out) var(--d, 0s), transform 0.8s var(--ease-out) var(--d, 0s);
  }

  html.js .in .rise,
  html:not(.js) .rise {
    opacity: 1;
    transform: none;
  }

  @media (prefers-reduced-motion: reduce) {
    .bg {
      animation: none;
    }

    .cue-line::after {
      animation: none;
      transform: none;
    }

    .line-inner,
    .rise {
      transform: none;
      opacity: 1;
      transition: none;
    }
  }

  @media (max-width: 900px) {
    .hero {
      min-height: 0;
    }

    .bg {
      background-position: 72% center;
    }

    .scroll-cue {
      display: none;
    }
  }
</style>

<script>
  // The hero is its own reveal root: mark it .in on load so the line-mask
  // and rise transitions play immediately. The timeout is a safety net for
  // environments where requestAnimationFrame is throttled.
  const hero = document.getElementById("top");
  const show = () => hero?.classList.add("in");
  requestAnimationFrame(show);
  setTimeout(show, 400);
</script>
